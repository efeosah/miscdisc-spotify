import React from 'react'
import { useAuth } from '../hooks/useAuth'


export const Dashboard = ({hash}) => {
    // const accessToken = useAuth(hash)

    return (
        <div>
            {/* {hash} */}
        </div>
    )

}